import { Placeholder } from "./Placeholder";

export function Settings() {
  return (
    <Placeholder
      eyebrow="System"
      title="Settings"
      description="Choose your LLM provider and model, manage keys, pick the embedding model, and set your Tavily and GitHub tokens — all stored locally."
    />
  );
}
