import { Placeholder } from "./Placeholder";

export function Profile() {
  return (
    <Placeholder
      eyebrow="Build your profile"
      title="Profile & Skills"
      description="Your identity, experience, education, projects, certificates, languages, and rated skills — everything the generator draws from."
    />
  );
}
