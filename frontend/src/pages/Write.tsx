import { Placeholder } from "./Placeholder";

export function Write() {
  return (
    <Placeholder
      eyebrow="Apply"
      title="Generate letter"
      description="Produce a grounded, personalized cover letter that streams token by token in your own voice."
    />
  );
}
