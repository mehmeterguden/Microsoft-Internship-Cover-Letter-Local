import { Placeholder } from "./Placeholder";

/** Dev-only gallery of every UI component in both themes. Filled in the components PR. */
export function ComponentsShowcase() {
  return (
    <Placeholder
      eyebrow="Dev"
      title="Component gallery"
      description="Every primitive and composite component, rendered in both themes to catch drift."
    />
  );
}
