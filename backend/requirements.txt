# ── Web framework ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── Local vector store ──
chromadb>=0.5.0

# Added in later phases as features need them:
#   sentence-transformers   (embeddings)
#   pdfplumber              (CV parsing)
#   openai                  (local LLM client)
#   python-docx, reportlab  (export)
#   tavily-python           (company research)
