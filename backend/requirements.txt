# ── Web framework ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9   # file uploads (CV PDF)

# ── Document parsing (CV: PDF, image, Word) ──
pdfplumber>=0.11.0        # PDF text
python-docx>=1.1.0        # Word (.docx) text
pytesseract>=0.3.10       # image OCR — requires the `tesseract` binary (brew install tesseract)
Pillow>=10.0.0            # image loading for OCR

# ── LLM provider clients (one per backend) ──
openai>=1.0.0       # OpenAI-compatible: Foundry Local, Ollama, OpenAI
anthropic>=0.40.0   # Claude
google-genai>=0.3.0 # Gemini

# ── Local vector store ──
chromadb>=0.5.0

# Added in later phases as features need them:
#   sentence-transformers   (embeddings)
#   pdfplumber              (CV parsing)
#   python-docx, reportlab  (export)
#   tavily-python           (company research)
