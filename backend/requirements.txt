# ── Web framework ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ── Local LLM client (OpenAI-compatible — Microsoft Foundry Local) ──
openai>=1.0.0

# ── Local vector store ──
chromadb>=0.5.0

# Added in later phases as features need them:
#   sentence-transformers   (embeddings)
#   pdfplumber              (CV parsing)
#   python-docx, reportlab  (export)
#   tavily-python           (company research)
